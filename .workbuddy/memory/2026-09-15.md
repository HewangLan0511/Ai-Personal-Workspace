# 2026-09-15

- **PW-INTEGRATION-001 整合前架构审计（只分析不改码）**：三路 Explore 实读（core/UI/Python+数据+插件）+ 承重结论二次复核。报告 `docs/reviews/PW-INTEGRATION-001-架构审计.md`。
- **核心发现**：①边界设计优秀但**底座空转**——motion token/views/components 零命中、cinema/interrupt/claim 零业务消费、Skin 引擎无 UI 入口（唯一生效=App.vue 页过渡+button 级兜底）；②Toast/Modal **三套自造**（mv-toast/apps-toast/saved-toast），Drawer/ContextMenu 不存在；③core api/mod.rs+commands.rs 双巨石 1800+ 行，双通道同源靠注释约定；④核心链路进入侧全 REAL（scheduler 七步流水线真实 CreateProcessW+Win32 布局），**退出侧断点**：快照仅内存仅模式级（runner.rs:142 last_snapshot），"完成工作"环节 [MISSING]。
- **四问全否**：换 Skin/加 Plugin/换 AI Provider/跨 UI 调 Workspace Engine —— 都不需要动业务逻辑/Core/UI（数据驱动+双通道+单写入者）。全项目最强资产=invoke 单点（`invoke(` 全局仅 api/client.ts:53）+ 事件单通道 + 前端零 DB 通路。
- **路线图 8 阶段**：IP1 浮层统一+Motion 接线 → IP2 UI Contract v1 → IP3 Motion 业务消费+Cinema 接真实链路 → IP4 Skin 产品入口 → IP5 Workspace 退出恢复（涉 DB 迁移 0009）→ IP6 core 双通道试点 → IP7 安全加固 → IP8 完成工作只设计不实现。三决策点留给白宇（恢复口径/Provider 入口/是否立项 #8）。
- **PW-INTEGRATION-002 Preflight 交付（只设计不改码）**：`docs/reviews/PW-INTEGRATION-002-preflight.md`。三个产品决策冻结：①退出恢复=进入前完整工作台窗口快照（给出 WorkspaceSnapshot v1 模型：desktop.windows[{hwnd,pid,exePath,rect,state,zIndex近似}] + workspace.managed；持久化建议先走 config 键 `workspace.snapshot.last` 零迁移，新表 0009 留后；降级链 L0 全成功→L1 部分 hwnd 失效→L2 显示器拓扑变→L3 快照损坏不猜→L4 超时中止；红线：只动快照登记的 hwnd、不杀进程）②AI Provider canonical=core config（`ai.provider.current`/`ai.model.current`），localStorage 降为缓存，模式 aiProfile 降为"建议层"，请求层只传不决策③"完成工作"登记为 PD-001 不编码。
- **UI-04 对接矩阵**：A 可直接接 4 项（学习目标入口/设备入口/独立 AI 页数据面/首页响应式）；B 需定契约 3 项（AI 页与侧栏会话共享+流式互斥、生活插件区域机制、档案扩展字段键值）；C Core 真缺口 2 项（**主应用窗口 resize 命令缺失**——core 只有 minimize_window commands.rs:41；profile_basic 无扩展列 0001_init.sql:88-95）；D 全部 8 项视觉面等 UI-04。**待白宇澄清："工作模式窗口 Resize"指外部软件窗口（零 Core 工作）/主窗口（加命令）/布局编辑器槽位拖拽（前端为主）**。
- **浮层统一结论**：canonical Toast 形态取 ModeView/SoftwareView 现有行为（4000ms+单条），SettingsView 静态 saved-toast 迁移为 success 短 toast，degraded-banner 不并入；Modal 两套（mv-modal/apps-modal）；Drawer/ContextMenu 属**真缺失**（不是页面没用）本轮不造。Motion 第一批接线：AiSidebar 拖拽接 claim('drag') 可先做（不等 UI-04），Cinema 等 UI-04 定稿。
- **新技能**：`big-repo-architecture-audit`（用户级 skill）——三路并行探查 brief 模板 + 承重结论二次复核 + 报告骨架 + 只分析不改码纪律，本轮第二次使用后沉淀。
- **PW-INTEGRATION-003 Contract Freeze 交付（只冻契约，零业务代码改动）**：`docs/reviews/PW-INTEGRATION-003-contract-freeze.md`。四决策转 Contract：①External Window Resize 复用 `windows_place`（**零 Core 新增命令**）②Snapshot 走 config 键 `workspace.snapshot.last`（零 migration）③AI Provider canonical = `ai.provider.current`（四层职责：canonical/cache/suggestion/transport，新增 L4 无 UI 调用者回退层）④PD-001 不实现。
- **新产物形态（本轮首次）**：契约不只写文档，还落成机器门禁 —— `docs/contracts/workspace-snapshot.v1.schema.json`（`additionalProperties:false` = v2 越界判据）+ 6 个 fixture（2 正向 / 4 反向，反例必须被拦）+ `tools/verify_contracts.py`（标准库零依赖静态门禁，**6/6 符合预期**）。**回固定动作**：往后任何"接口冻结"都走「文档 + JSON Schema + 正反 fixture + 静态门禁」四件套，不靠肉眼相信"边界已写清"。
- **本轮新发现（已写进契约，改变后续施工）**：①`ai.default_provider` 是**活键**（`pluginHost.ts:117` / `agents.rs:352` / `settings.ts:23` 三处消费）——002 曾漏此层；②registry **无默认兜底**（`ai/service.py:94` 对 `""` 抛 KeyError）→ 插件/外部 Agent `ai.invoke` 在默认空值时必失败（缺口 C3）；③config 键有**白名单硬校验**（`config.rs:41-43`）→ 三个新键必须 `KEYS`+`expected_type`+`default_for` 三处齐登，漏 `expected_type` 静默变"无类型校验"；④`windows_place` 的 `alwaysOnTop` **实际无效**（`window.rs:172-175` 重复置位，登记不修）；⑤快照采集可直接复用 `modes_capture_current_inner` 的枚举+过滤+归一化。
- **关键裁决（易踩错）**：A→B 模式切换**禁止覆盖**持久快照 —— 否则退出时恢复到"上一个模式的工作态"而不是普通工作台。持久快照（`enter_mode` 写，恢复工作台）与内存快照（`before_switch` 写，回到上一模式，`runner.rs:142`）**职责必须分离**。
- **新技能**：`contract-freeze-with-fixtures`（用户级 skill）——「文档 + JSON Schema + 正反 fixture + 零依赖静态门禁」契约冻结作业法。
- **TECH-02 Workspace Runtime 交付（验收 11/11）**：把 UI-05-B 的"UI 直读 mock"改成「UI → `workspaceRuntime` 门面 → 内存 store」。三模块 `ui/src/workspace/{store,layout,runtime}.ts`（store 不 import Vue，用显式 `subscribe()`；全返回 `deepFreeze(clone())`；`__SEED_TEMPLATES` 模块私有不导出 = "UI 不直连 mock"的**结构级**保证）。参考 UI/唯一消费者 `ui/src/views/DevWorkspaceHarness.vue` 挂 `/dev/workspace`（dev-only，不进导航）。验收 `tools/verify_tech02_workspace.py`（T1~T6，11/11，Edge headless + CDP，**跑在 `ui/dist` 最终产物上**，脚本自带"dist 早于 src 即 FATAL"门禁）。文档 `docs/tech/workspace-runtime.md`。
- **本轮最重要的一条经验（已写进 `edge-headless-ui-verify` skill v1.1.0）**：**框架托管的 `:style` 会冲掉命令式写回的几何 → restore 静默失效**。save→move→restore 用例第一次就变红（`moved=true, restored=false`）：`loadLayoutSnapshot()` 用 `applyLayoutToDom` 把 DOM 写回 A，紧接着 store 通知触发 Vue re-render，Vue 又用组件镜像里的旧值 B 把 DOM 冲回 B。修法：**几何一律命令式写 DOM，模板不绑几何 `:style`**；镜像只作数据，并在每次 store 变更后做一次「DOM 赢」对账（把真实 DOM 几何收回镜像，否则下一次重放又把旧值写回去）。一般化：任何"回放/恢复/导入布局/还原配置"类功能，只要渲染层还在托管同一批属性，命令式写回就会被下一次 re-render 覆盖。
- **验收脚本纪律（本次实测有效）**：①就绪门用"窗口真的渲染出来 + 几何可测"而不是"标记非空"；②T5「不整页重绘」用**节点身份 `===`**（不是"标记还在"）+ **对照组**（换模板时节点身份必须变）证明探针有区分度；③T2 用"reload 后自定义模板消失、种子回来"作**零持久化**的双向证据（比读代码断言强）；④T4 加"未匹配 windowId 不改任何状态"作对照组，防"事件路径恒置 running"的假通过；⑤单项 try/except，失败不连坐，并打印原始异常。

- **TECH-03-A AI Model Management Architecture 交付（只建层，验收 31/31）**：把"当前用哪个 AI 模型"的**五个来源**（S1 localStorage `ui.ai.provider`/`ui.ai.model` 读源 / S2 core 同名镜像键只写不读 / S3 `work_modes.ai_profile` 建议 / S4 `ai.default_provider` 无头默认 / S5 `AiChatArgs` 请求级）统一到一个读写门面后面。四文件 `ui/src/ai/model/{model,provider,registry,ports}.ts`（589/472/823/437 行），验收 `tools/verify_model_registry.py` 31/31，文档 `docs/tech/model-management-runtime.md`。
- **本层架构要点**：①**领域层零外部依赖**（model/provider/registry 不 import Vue、不 import `@/`）→ 项目自带 tsc 编成 CommonJS 直接在 Node 跑 T1~T5（TECH-01/02 验 UI 用 Edge headless；纯领域层不用浏览器）；IO 全收进 `ports.ts`（唯一 import `@/api` 的文件）②**零新增后端**：不新增 core 命令、不新增 config 键、不迁移 DB —— canonical 写序 L1(`ai.provider.current`) → 落不住则**过渡镜像键**（已登记的 `ui.ai.provider`/`ui.ai.model`，也正是 `stores/ai.ts` 在写的两个键）→ L2 缓存，如实回报 `source/stale/pendingSync` ③**默认模型不是 ModelProfile 字段**，由 canonical 推导（`getDefault()` → `findByCanonical()`），删默认项 canonical 不动、进 dangling 状态，同 id 再 add 自动认领 ④`TASK-04` 门面刻意做窄（`createModelsApi`），UI 拿不到 store/canonical 写口/ports。
- **本轮 4 类真实缺陷（都被验收脚本抓出）**：①`deriveModelId` 的 `sanitizeIdPart` 把 `:` 压成 `-` → 本地模型 tag `qwen2.5:7b` 变成 id `ollama:qwen2.5-7b`，与真实模型名对不上（修：白名单加 `:`，id 是不透明键，按**第一个** `:` 切分）②`add()` 先查 Provider 表再报结构错 → 空 provider 报含糊的 `provider_unknown`（修：结构错误前置，报 `empty_provider`/`empty_model`）③endpoint 协议检查对 api/local 各报一次 → 同一问题两条 `endpoint_invalid`（修：只在 local 分支补查）④`maskOf` 重复定义（ports.ts + model.ts），移到纯层 `model.ts` 供 Node 测，ports.ts 改为 import + 便利再导出。
- **验收脚本自身的 3 个 bug（假红，不是产品问题）**：①`setCanonical` 是 `async`，`throwsWith` 同步 try/catch 抓不到 rejected Promise（连 unhandled rejection 都没炸）→ 新增 `rejectsWith(promise, code)` 异步版 ②T4c `sizeBefore` 在注册 3 个自定义 Provider **之后**才取（应为之前），且幂等重登记只传 `{id}`，**把描述符清空** → 连坐打挂 T4d（默认模型丢失）→ 修为 `toInput` 全量重放 ③T5a 「local 既无 endpoint 也无 localPath」用 `add()` 测，但 `add()` 会用 Provider `defaultEndpoint` 兜底（正确行为）→ 改为在**纯校验层** `validateModelProfile` 直接验规则本身。教训：**测"规则"要在规则所在的层测，别拿上层的兜底行为当"规则失效"；异步拒绝必须有异步断言器**。

---

## TECH-03-B · 已有 Runtime 接线准备（已交付，验收 81/81）

**定位**：停止造设施，开始接线。四件事全部完成。

- **§一 Motion 首批业务接线**：`AiSidebar.vue` 宽度拖拽改由 `claim(el,'drag', onSuperseded)` 仲裁
  （拿不到 `isPrimary` 即不启动；move 侧 `if (!dragClaim?.isPrimary) return`；`endResize()` 里 release 与拆监听成对）。
  `motion/runtime.ts` debug 句柄补出 `hasActiveClaims`（可观测）。**零新增动效**——验收直接断言
  AiSidebar 代码里不存在 transition/animation/@keyframes/transform。
- **§二 统一 Toast**：新增 `composables/useToast.ts` + `components/ToastHost.vue`（唯一渲染者，App.vue 挂载）。
  三套旧实现归零（ModeView 19 处 / SoftwareView 13 处 / SettingsView 5 处调用点迁移）。
  **视觉像素级不变**的做法：`.toast-canonical` 的 10 条声明**逐字**取自旧 `.mv-toast`/`.apps-toast`；
  `success/error/info` 只写进 `state.variant` + `data-variant`，**CSS 里不加任何按变体着色的规则**。
  删掉 `.saved-toast`。**degraded-banner 刻意不并入**（它是持续的系统降级状态，不是 4 秒瞬时提示）。
- **§三 快照接口准备**：新增 `workspace/snapshot.ts`（907 行，**零 import**，Node 可直驱）。
  `capture()` 无探针时返回 `dryRun:true / degraded:['probe-not-wired']`——**拒绝伪造数据**；
  `restore()` **只算不做**（每步 `executable:false` + `guardrails` 四红线全 false）；
  `runId` 不匹配 ⇒ hwnd 视为失效 ⇒ 全步降级 `skip`。`SNAPSHOT_V2_FORBIDDEN` 10 项与冻结 schema 逐项一致。
- **§四 模型只读消费者**：新增 `ai/model/consumer.ts`（`ModelReadOnly` **Object.freeze**，`MUTATOR_NAMES` 供断言）
  + `composables/useCurrentModel.ts`（单例 Registry、懒启动、失败不抛 UI）+ AI 侧栏顶部只读「当前模型」
  （`data-pw-current-model`）。**只读做成类型约束**：消费者拿不到任何写方法。顶栏四态如实显示
  （未配置 / 正常 / 已失效 / 待同步 / 离线），**不连真实 API**（不调 `check()`、不 import `createModelsApi`）。

**回归**：`gate.py --stage 9 --build` FAIL=0 WARN=0 PASS=30；contracts 6/6；tech01 9/9；skin 12/12；
tech02 11/11；model_registry 32/32；perf 12 场景 error=0（longtask 全 0）；verify_tech03b 81/81。

**本轮踩到的两件事（值得记）**：
1. **边界被有意扩张时必须显式记账**：`verify_model_registry.py` 的 `T6b` 原是"零接线"，
   与 TECH-03-B §四 直接冲突 → 升级为"接线点白名单 + 白名单内不得写"（新增 `T6b2`），
   并在报告与台账**显式写明这是扩张**。不要静默放宽上一轮的判据。
2. **又一次"假失败"（第 N 次）**：`verify_tech03b.py` 首跑 79/81，两个 FAIL 全是**脚本 fixture 缺陷**
   ——`snap-k` 的 managed 只放 1 个窗口导致"排序"无从验证；`snap-o` fixture 自相矛盾
   （managed 说软件没跑、`foregroundHwnd` 却指向在跑的窗口 → 计划多出一条**合法**的 focus-window）。
   处置口径：**脚本红但产品对 ⇒ 改脚本，不许为了变绿放松产品断言**。

**下一阶段（非本轮遗留）**：① core 登记 L1 键 `ai.provider.current`/`ai.model.current`；
② 设置页接管 canonical 写口（在那之前**只显示不改行为**，否则会"顶栏显示 A、实发 B"）；
③ core 实现窗口探针 → `capture()` 转 `dryRun:false`；④ `snapshot.ts` 挂 `workspaceRuntime` 门面时
须同步改 `verify_tech02_workspace.py` 的 `T1c` 消费者白名单。


---

## TECH-04 · 核心能力接入规划与第一阶段落地（2026-09-15 下午）

**交付**：`docs/tech/tech04-core-integration-phase1.md`（架构变化报告 / 数据流说明 / 风险列表 / 回归测试 四段齐）；
设计 `docs/tech/workspace-work-lifecycle-design.md`；验收 `tools/verify_tech04.py` **44/44**。

### Phase 1 · AI 模型真实闭环（消除「显示模型 ≠ 实际调用模型」）
- 断点：Model Registry 从未参与 → "当前模型"无唯一事实来源；core 按自己规则选模型；canonical L1 键从未在 core 登记。
- **canonical 落点定在 L1**：`core/src/db/config.rs` **三处齐改**（`KEYS` 106-107 / `expected_type` 166 / `default_for` 224-225），
  漏一处该键就是死的；`CONTRACT_SCHEMA_VERSION` 11→12（**无 schema 变化，未加迁移**）。
- **镜像兼容**：`ui.ai.provider`/`ui.ai.model` 继续写、不删旧值（老版本回滚可读）；canonical 是**新增权威**不是替换。
- **迁移路径**：老数据只写过镜像 ⇒ `source=mirror` ⇒ 启动时 `resolveAtBoot()` **前向拷贝**到 L1，旧值原地保留、幂等。
- **分层**：`selection.ts`（纯决策·零 IO，157 行）/ `session.ts`（纯逻辑·注入 registry，221 行）/
  `bridge.ts`（薄接线·持单例，95 行）。`stores/ai.ts` 与 `useCurrentModel.ts` **改用同一 registry 单例**
  → 顶栏显示与 `send()` 请求依据读同一个 `current()`，**分叉在构造上不可能**。

### Phase 2 · Snapshot 接入准备（只准备，不动窗口）
- `workspaceRuntime` 新增 `snapshot` 命名空间 + `getRecoveryStatus()`：三视图（当前工作空间 / 最近布局 / 恢复可用性），
  全部只读 + `deepFreeze`。
- **`executable` 恒 `false`** + 四条 `guardrails` 恒 `false`（不杀进程/不启软件/不写库/不碰未登记窗口）；
  命名空间内**无执行方法**。`capture()` 未注入探针时恒 `dryRun`；`restore()` 只算计划。
- `T1c` 消费者白名单**未改**（`snapshot.ts` 是内部模块，唯一 import `runtime.ts` 的仍是 `DevWorkspaceHarness.vue`）
  —— 兑现了 TECH-03-B 待办④的预判（原以为要改，实测不用）。

### Phase 3 · 工作完成概念（纯设计）
- 五态 `idle→starting→running→paused→completed→archived`（T1–T10 转换）+ 7 不变量 + 四红线 + 三模式差异。
- **零代码、零 UI**。

### 回归（全绿）
`gate.py --stage 9 --build` FAIL=0 WARN=0 PASS=30 · core `cargo test` 86/86 · tech04 44/44 ·
tech03b 81/81 · model_registry 32/32 · contracts 6/6 · tech01 9/9 · skin 12/12 ·
tech02_workspace 11/11 · perf 12 场景 longtask 全 0。

### 本轮两个值得记的点
1. **上游判据"边界翻转"必须显式记账**：canonical 落 L1 后，`verify_model_registry.T6d` 与
   `verify_tech03b.T5b` 由"L1 未登记"翻转为"三处登记 + 镜像保留"；`T6b` 白名单加 `stores/ai.ts`；
   `verify_stage9` 预检 `==11` 放宽为 `>=11`。全部写进报告 §4.3 与台账，**不许静默放宽**。
2. **对照组这次是硬要求**：迁移类判据最容易写成"反正都成功" → 强制配 `t2-g`（L1 未登记时迁移**确实失败**）
   证明 `t2-d` 有区分度；`t1-l` 同理做 `t1-k` 的对照组。
3. **`verify_tech02_workspace.py` 首跑报 `dist 早于 src`**：是它与 `vite build` **并发**导致的时序问题，
   不是产品缺陷 —— build 完成后重跑 11/11。该脚本"拒绝在过期产物上验收"是**有意的**（验收必须跑在最终产物上）。

### 下一阶段建议（非本轮遗留）
① 设置页接管 canonical 写口（闭环）；② core 实现窗口探针 → `capture()` 转 `dryRun:false`；
③ `restore()` 执行侧放开时 `executable`/`guardrails` 才允许变受控开关；④ Phase 3 状态模型 UI 落地另行立项。
