# 2026-09-17

## TECH-07-C7 Phase 2 状态（00:56 暂停，明天续）
- 产品改动已全部完成并两轮验证：RunView.vue（Esc链/预览吸附/MIN统一/卡片补全）+ actions.ts（常量+联合clamp）+ index.ts（re-export）。typecheck 绿，dist 已 VITE_CORE_BASE='' 同源重建。
- verify_tech07c7.py 两轮全套：S 13/13、D 6/6 全绿；R 段红灯均为深层嵌套（c7→c6→c5→c4 四层串行）资源争抢轮转，非产品问题。
- run2 后已补证据：**c4 空闲单跑 25/25 通过**（_c7_c4_solo.log）。
- **明天待办**：① 单跑 c5（python tools/verify_tech07c5.py，预计 ~10min）；② 通过后写 docs/tech/TECH-07-C7-implementation-report.md + LEDGER.md C7 条目（C5/C6 条目已补）；③ 停止点：C7 收口，不进 C8。
- 注意：全套 R 段四层嵌套易偶发，收口口径=『每套有独立通过证据即可，不追全套 27/27 连绿』（用户已认可终止条件式收口）。

## C7 收口（10:4x）
- c5 空闲单跑 24/24（嵌套 c4 亦 25/25）——最后一块通过证据到位，R 段八套全绿。
- 已写：docs/tech/TECH-07-C7-implementation-report.md；LEDGER 补 C5/C6/C7 三条。
- **TECH-07-C7 完成，停止点：不进 C8。** 四层嵌套验证偶发结论：红灯轮转+空闲单跑全绿=资源争抢，非产品问题；收口口径已写入报告。

## V0.1-FINAL 收口（12:24~15:0x）
- Phase 1 只读审计 + Phase 2 执行完毕：dist 779→41、临时日志清理、emptyOutDir:false 保持（方案 A）、git 基线快照→清理→构建→回归→Smoke→最终 commit + tag v0.1。
- 关键发现：沙箱对嵌套三层 spawn（套件互调重跑）间歇拦截 core.exe/Edge 执行 = 本日嵌套回归红灯根因；单跑全绿、c7 单跑 27/27（历史首次）为收口证据。后续跑套件如再遇嵌套红，先看是否沙箱拦截再怀疑产品。
- 交付：docs/tech/V0.1-FINAL-audit.md + V0.1-FINAL-report.md；LEDGER V0.1 条目；tools/_v01_* 证据日志留档。
- 冻结：V0.1-FINAL complete，no C8。

## UI 保真收口 ①「顶部壳层 appbar 化」（23:47 起 → 09-18 00:20 完成）
- 交付：`App.vue` 新增 `.app-titlebar`（TopBar+ModeBar 合成**一条** 46px appbar）；`TopBar.vue` 重写为原型 `.titlebar` 结构（logo/sep/context/spacer/动作区）；`router/index.ts` 导出 `PAGE_TITLES` 给 `.tb-context`；`base.css` 加 `.app-titlebar`/`.app-topbar` 骨架 + `.tb-*` 原子类 + `.mode-bar` 内联收编 + `.app-main` 取 `--bg-canvas`。
- **关键手法（下次照用）**：要改"冻结 hash 的组件"（`ModeBar.vue`）的视觉时**别改文件** —— 用 base.css 三段选择器（0,3,0）覆盖其 scoped（0,2,0），再用 **computed style 实测**证明覆盖生效。冻结基线保持原值＝比"改基线记账"更强的证据。
- 新脚本 `tools/_force_appbar_check.py`（14 项：单条 appbar / 覆盖生效 / 右缘对齐 / 窄窗 1024 零溢出 / 暗色跟随 token / 验收文案）。**写法要点**：路径用 `Path(__file__).resolve().parents[1]`、Edge 用环境变量推导 ⇒ 门禁 A020/A021 零命中（别学老 `_force_*` 脚本写死 `C:\Users\...`）。
- 记账：`base.css 3558c87215e7bd74 → 4c95f4fc3b0bd16d`（c2/c3/c4/c5/c6 **五处同步**并复跑各自 hash 检查全 PASS）；Tauri release 重建（touch `core/src/main.rs`）×2；**NSIS 刷新 10,763,264 → 11,383,808 B**；应用已重启为可用状态。
- 踩坑：**Tauri 的 `beforeBuildCommand` 会重跑 `npm --prefix ui run build`** ⇒ Tauri 构建后 `ui/dist` 已是**生产构建**（chunk 名与 `_fusion_build.py` 的验证构建不同）；探针跑生产 dist 才算真证据。
- **新发现（登记 LEDGER L-047）**：`gate.py --stage 9 --build` 实测 **FAIL=40 / WARN=116 / PASS=30** —— 40 条全是 A020/A021 且**全在 `tools/_*.py`**（协议自己要求的 `_fusion_build` / `_force_motion_check` / `_force_refresh_install` 都在其中），产品代码 0 命中。CONTEXT-PACK 的"0F/0W"基线**已失真**，待白宇裁决 A（改工具路径写法）/ B（A020/A021 排除 `tools/`）。
- 另记：`verify_tech04` **41/44**（T4a/T4b/T3g 为历史快照未随 TECH-07-C1 / UI-FUSION-REAL 更新）；`_force_ui_dom` **6/12**（4 页源码里没有该 marker 类 + 2 页 marker 依赖列表数据 = 无 core 则为空）⇒ **均属 ② 的施工范围**，非本批引入。
- 下一批 = **② Life/Device/Plugins/Learning/Project 六页内部卡片原型化**（顺手补 `.page-head`/marker 类，可让 `_force_ui_dom` 一并转绿）。

