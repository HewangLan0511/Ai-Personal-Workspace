# prompt/ —— AI 提示词模板集中管理（阶段5 交付）

详见 `docs/agent-dev/08-阶段指令-AI助手.md` §4。

模板以 `.md` 文件存放，渲染器变量语法 `{{变量名}}`，例如：

```
[系统] 当前工作模式：{{current_mode}}
       已打开软件：{{apps}}
       项目目录：{{project_dir}}
```

禁止散落在代码里。阶段1 占位。