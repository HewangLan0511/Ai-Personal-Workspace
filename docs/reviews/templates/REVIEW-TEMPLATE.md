# 审核报告 REVIEW-XXX · 阶段N：<阶段名称>

| 项目 | 内容 |
|------|------|
| 审核编号 | REVIEW-XXX |
| 审核日期 | YYYY-MM-DD |
| 审核阶段 | 阶段 N · <名称> |
| 审核对象 | <改动文件范围 / 交付包> |
| 审核基准 | `<指令文件>` 、`SUPERVISOR.md` 六维清单 |
| 审核方式 | 自动门禁 + 人工核查 |
| **判定** | **✅ 通过 / ⚠️ 有条件通过 / ❌ 驳回** |

---

## 一、审核范围与基准

### 覆盖
- <列出实际审核的文件/模块>

### 不覆盖
- <明确写出本次没审的部分，避免误认为已通过>

### 事实基线
```
<实际文件树 / 命令输出，不要写推测>
```

---

## 二、自动门禁输出（原始）

命令：
```
python tools/gate.py --stage N --build
```

```
<粘贴原始输出，不许改写为"通过">
```

**门禁结论：X FAIL / Y WARN / Z PASS**

> 若本次对门禁工具本身做了修改，必须在此说明改了什么、为什么改、是否重测。

---

## 三、逐条验收（对照阶段指令的验收表）

| # | 验收项 | 结果 | 证据（路径+行号 / 命令+输出） |
|:-:|--------|:----:|------------------------------|
| 1 | | ✅/❌ | |
| 2 | | ✅/❌ | |
| 3 | | ✅/❌ | |

**核心验收：X/Y 通过**

> 证据栏不允许写"看起来没问题""代码里实现了"。必须是**可被第三方复现**的东西。

---

## 四、六维审核

### 4.1 结构维
- [ ] 目录结构符合 `02-架构与目录规范.md` 2.2
- [ ] 必需文件齐全
- [ ] 无构建产物 / 数据库文件混入源码

### 4.2 架构维
- [ ] `core/` 不依赖 `modules/` `plugins/` `ui/`
- [ ] `ui/` 不直接读写 SQLite
- [ ] `plugins/` 不直接访问数据库
- [ ] 数据库写入只在 core（单一写入者）
- [ ] 跨模块走 Event Bus
- [ ] 事件名/表名/权限名走常量
- [ ] ADR-001 已定稿且全项目一致

### 4.3 契约维
- [ ] 新增表/JSON/事件/接口已登记到 `03-数据契约与接口规范.md`
- [ ] 表结构变更提供 migration（只增不改）
- [ ] `schema_version` 已更新
- [ ] 旧数据升级路径已实测

### 4.4 体验维
- [ ] 失败路径有用户可读提示
- [ ] 异步操作有 loading 状态
- [ ] 长耗时操作可取消或有进度
- [ ] 设置项重启后保持
- [ ] 空状态有引导

### 4.5 安全维
- [ ] 无明文密钥
- [ ] 插件权限默认拒绝
- [ ] 破坏性操作有二次确认
- [ ] 聊天内容未落库
- [ ] 咨询模式无法读取用户数据

### 4.6 文档维
- [ ] 新模块有 README
- [ ] 验收步骤可复现
- [ ] 已知限制已列出

---

## 五、发现的问题

### F-01 · <标题>（级别：阻断 / 严重 / 一般 / 建议）
**描述**：
**影响**：
**要求**：
**登记**：L-0XX

---

## 六、一票否决项检查

| 红线 | 检查结果 |
|------|----------|
| V1 明文密钥 | ✅ 未命中 / ❌ 命中 / ⚪ 不适用 |
| V2 咨询模式读取用户数据 | |
| V3 AI 自动改档案/进度 | |
| V4 插件默认权限 | |
| V5 破坏性操作无确认 | |
| V6 谎报完成 | |
| V7 插件直连数据库 | |
| V8 构建产物入库 | |

**红线命中：N**

---

## 七、判定与理由

### 判定：<通过 / 有条件通过 / 驳回>

**理由**：
1.
2.

**若有条件通过 —— 遗留项清单**：

| ID | 描述 | 级别 | 是否阻塞下游 | 修复阶段 |
|----|------|:----:|:------------:|:--------:|
| | | | | |

> 有条件通过**不得**用于：核心功能不完整、架构违规、红线命中。

---

## 八、返工要求（驳回时必填）

必须列出**可执行**的返工项，每条都能被验证：

| # | 返工项 | 完成判据 |
|:-:|--------|----------|
| 1 | | |
| 2 | | |

> "再检查一下""优化一下代码"这类无法验证的要求，禁止出现在返工清单里。

---

## 九、下一步

**开发 Agent 下一步动作**：

**送审前必须准备**：

**触发口令**：「申请审核阶段 N」

---

*审核人：肉编器001号（监制） · YYYY-MM-DD*
