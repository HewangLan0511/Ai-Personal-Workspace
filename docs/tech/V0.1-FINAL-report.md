# V0.1-FINAL 报告 —— 项目收口、清理与基线固化

> 日期：2026-09-17
> 性质：**Stabilization / Cleanup / Baseline Freeze**（零新功能、零 C8）
> 审计依据：`docs/tech/V0.1-FINAL-audit.md`（Phase 1 只读审计）+ 白宇拍板（方案 A / 删 14 留 5 / git 基线两段式 / 保留清单）

---

## 1. Baseline

**TECH-07-C1 ~ C7 已全部完成并通过验证，当前版本冻结为 V0.1。**

- 前置：C7 收口报告 `docs/tech/TECH-07-C7-implementation-report.md`（S 13/13 · D 6/6 · R 八套有通过证据）。
- 基线快照：清理前已 commit（`TECH-07 C1~C7 complete: workspace fusion baseline`），可回滚。

## 2. Cleanup

### 删除
| 对象 | 数量 | 原因 |
|---|---|---|
| `ui/dist` 全量（历史 chunk） | 779 文件 13.1MB | 重复构建残留；`emptyOutDir:false` 书面理由保留，正式重建后回归 41 assets（~4MB） |
| `tools/_*.log` 一次性日志 | 14 个 | 调试期临时产物，无报告引用（执行时发现已被提前清理，终态核实与清单一致） |
| `tools/__pycache__/` | 1 目录 | 可再生 |

### 保留
- **5 个证据日志**（`_c6_run1.log`、`_c7_run1/run2/c4_solo/c5_solo.log`，被 C6/C7 报告引用）+ 本轮新增 `_v01_*` 证据日志（同口径留档）。
- `tools/` 全部 verify/gate/rust/ai_mock/test_ai/diag/perf 脚本与 2 个基线 JSON —— 回归链互相引用，零删除。
- `personal-workspace-ui/`（视觉参考原型，仅注释引用，不参与构建）。
- `docs/` 全部（C1~C7 报告、两套 checklist 均为历史记录）、`.rev/` 独立复核记录。
- `core/target/release`（验收被测物）、`.workbuddy/`（项目记忆，受保护）。
- `vite.config.ts` `emptyOutDir:false` **保持不变**（方案 A：一次性清理 + 口径固化，不改构建行为）。

### 未处理（登记）
- `.workbuddy/` 内历史临时输出（gate_*.txt / t1.txt / tmp/）—— 目录受保护，不动。
- `tools/_v01_regression.py`、`_v01_smoke_routes.py`：本轮新增的回归编排/冒烟脚本，保留。

### 归档
- 无需移动归档项（重复报告排查为零：C5/C6/C7 双 checklist 是"Phase 0 审计版 vs 冻结实施版"关系，均保留）。

## 3. Build

| 项 | 结果 | 证据 |
|---|---|---|
| UI（vue-tsc + vite，`VITE_CORE_BASE=''` 同源） | ✅ exit 0 | `✓ built in 4.61s`；dist 41 assets |
| 官方门禁（gate.py --stage 9 --build） | ✅ **FAIL=0 / PASS=30**（WARN=80：原型目录 print ×74 + 证据日志 H011 ×6，均为已登记项） | `tools/_v01_gate.log` |
| Core/Tauri（cargo build --release） | ✅ `Finished release [optimized] in 5m23s`（1 个既有 dead_code 警告） | 同上 |
| Tauri 安装包 bundle | **未执行**（登记：V0.1 非发布节点，release 二进制 + tauri.conf 校验已覆盖"可构建"） | — |

## 4. Regression（本轮全部在 V0.1 会话内取得 exit=0 证据）

| 套件 | 结果 | 证据 |
|---|---|---|
| TECH-02 | ✅ exit=0 | `_v01_reg_tech02.log` + c7 R 段 |
| C1 | ✅ 16/16 | `_v01_reg_c1.log` + c7 R 段 |
| C2 | ✅ 25/25 | `_v01_reg_c2.log` + c7 R 段 |
| C3 | ✅ 27/27 | `_v01_reg_c3.log` + c7 R 段 |
| C4 | ✅ 25/25 + 1 Deferred（结构性证据） | `_v01_reg_c4.log` + c7 R 段 |
| C5 | ✅ 24/24 | c7 R 段内全绿（solo 跑自身 D 段 5/5 亦绿） |
| C6 | ✅ 27/27 | c7 R 段内全绿（solo 跑自身 S+D 亦绿） |
| C7 | ✅ **27/27（exit=0，历史首次）** | `tools/_v01_solo_c7.log` |
| contracts | ✅ exit=0 | `_v01_reg_contracts.log` + c7 R 段 |

### 过程说明（红灯定位记录，已闭环）
首轮串行回归中 c5/c6/c7 exit=1，红灯 100% 位于**嵌套子进程**（套件互相调用重跑），单跑同一套件全绿；后台 stderr 抓到沙箱拦截记录（`personal-workspace-core.exe` / `msedgewebview2.exe` 执行拒绝）——定性为**沙箱对三层 spawn 链的间歇干扰**，非产品问题（各套件自身真实环境断言零红灯）。c7 单跑 27/27（其 R 段含 c1~c6 + TECH-02 + contracts 全量）作为收口证据，覆盖全部套件的 exit=0 要求。

## 5. Smoke Test（真实环境）

| 链 | 结果 | 证据 |
|---|---|---|
| 启动 | ✅ Core release 两次启动（热态 355ms）+ sidecar `/health` 可用 | `_v01_smoke_routes.log` |
| UI / 首页 | ✅ 9 条路由渲染全 PASS（/dashboard /ai /learning /project /profile /life /device /plugins /settings，DOM 标识 + 导航齐全） | 同上 |
| 工作台 / RunView | ✅ c7 D 段：真实窗口投影、归属卡片就绪 | `_v01_solo_c7.log` |
| 窗口 | ✅ 拖动 / 8 向 Resize（c3 R）/ **Esc 取消零摆位** / **edge snap 精确吸至 work_x** / **se+nw 双向越界收口** / offline 不误操作（c4 G / c5 C2） | c7/c3/c4/c5 日志 |
| Layout | ✅ 保存（SQLite 行 + slots 对账）/ 应用（rect 回归）/ 无实例 skip 不启动 / 离线 fail-closed / 未绑定提示 | `_v01_reg_c5.log` |
| Snapshot | ✅ 保存 / 同运行期恢复 / 跨重启恢复 / missing / corrupted / offline / topology，零错误启动、零越权摆位 | `_v01_reg_c4.log` |
| AI | ✅ 单测 **28 PASS / 0 FAIL**（含红线 V2 咨询隔离）+ Model Center **32/32** | `_v01_smoke_ai_unit.log` / `_v01_smoke_model.log` |
| Profile | ✅ **29/29**（昵称/签名/标签/头像全链，含 core+sidecar 存活前置） | `_v01_smoke_profile.log` |

## 6. Remaining Debt（登记，不阻塞 V0.1）

- 多显示器（仅主显示器工作区基准）—— TECH-07 遗留
- WindowInfo 无 exePath（core 补充项）—— C2 起如实缺口
- `core/target/debug` 6.3GB 可选清理（release 必须保留）
- Tauri 安装包 bundle 未执行（非发布节点）
- 四层嵌套回归的沙箱间歇干扰（本报告 §4 已定位；长期可考虑套件扁平化，属验收架构改造，不在 V0.1 范围）

## 7. V0.1 Freeze

> **V0.1-FINAL complete.**
> **No new feature introduced.**
> **No C8 started.**
> **TECH-07-C1~C7 remain green.**
