# plugins/ —— 第三方插件目录

每个插件一个独立子目录：

```
plugins/
└── com.example.<name>/
    ├── manifest.json
    ├── main/
    ├── ui/
    └── data/
```

## 阶段1 状态

空目录。详见 `docs/agent-dev/12-阶段指令-插件与小组件.md`（阶段9 交付）。

## 验证插件架构成功的标准（声明性 · 非可测试）

> 写一个新插件，零核心代码改动即能跑起来。

阶段9 落地时用此标准验证。