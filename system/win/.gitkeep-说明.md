# win/ —— Windows 专用实现（ADR-001 后仅媒体与性能）
#
# ADR-001 定稿：process.py / window.py 不在此目录（窗口与进程控制归 Rust core）。
# 阶段8 将实现：
#   media.py —— SMTC 媒体会话（winsdk / winrt，仅 stage 8 引入依赖）
#   perf.py  —— CPU/GPU/内存性能计数
